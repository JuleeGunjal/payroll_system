<div class="col-6" style="margin : 0 auto;">
  <h2><%= @tax_deduction.id %>'s details</h2>
  <h4>employee Name: <%= Employee.find(@tax_deduction.employee_id).first_name %></h4>
  <h4>ID: <%= @tax_deduction.id %></h4>
  <h4>Financial Year: <%= @tax_deduction.financial_year %></h4>
  <h4>Tax Type: <%= @tax_deduction.tax_type %></h4>
  <h4>Ammount: <%= @tax_deduction.ammount %></h4>

  
 
  </br></br>
  <div class="d-flex justify-content-center">  
    <div class="col-2">
      <%= link_to "Edit", edit_tax_deduction_path(@tax_deduction), class: "btn btn-warning"%> 
    </div>
    <div class="col-3">
      <%= button_to "Destroy", tax_deduction_path(@tax_deduction), class: "btn btn-danger", data: {:confirm => 'Are you sure?'}, method: :delete %>
    </div>
  </div> 