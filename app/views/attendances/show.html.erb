<div class="col-6" style="margin : 0 auto;">
  <h2><%= @attendance.id %>'s details</h2>
  <h4>employee Name: <%= Employee.find(@attendance.employee_id).first_name %></h4>
  <h4>ID: <%= @attendance.id %></h4>
  <h4>month: <%= @attendance.month %></h4>
  <h4>working_days: <%= @attendance.working_days %></h4>
  <h4>unpaid_leaves: <%= @attendance.unpaid_leaves %></h4>
 
  </br></br>
  <div class="d-flex justify-content-center">  
    <div class="col-2">
      <%= link_to "Edit", edit_attendance_path(@attendance), class: "btn btn-warning"%> 
    </div>
    <div class="col-3">
      <%= button_to "Destroy", attendance_path(@attendance), class: "btn btn-danger", data: {:confirm => 'Are you sure?'}, method: :delete %>
    </div>
  </div> 
</div> 